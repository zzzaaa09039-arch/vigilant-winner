import {client} from "./_openai.js"; export default async function(req,res){if(req.method!=="POST")return res.status(405).json({error:"POST فقط"});try{const {text,voice="alloy"}=req.body||{};const a=await client().audio.speech.create({model:"gpt-4o-mini-tts",voice,input:text});const b=Buffer.from(await a.arrayBuffer());res.setHeader("Content-Type","audio/mpeg");return res.status(200).send(b)}catch(e){return res.status(500).json({error:e.message})}}